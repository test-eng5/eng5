# 🛡️ Telegram Anti-Ad & Anti-Spam Bot

Умный Telegram-бот для автоматического обнаружения и блокировки рекламы, спама и мошеннических сообщений в группах и каналах.

## ✨ Особенности

- **🤖 Автоматическая модерация** - бот самостоятельно удаляет рекламу и спам
- **🛡️ Антифлуд система** - защита от слишком частых сообщений
- **🔍 Умный детектор** - распознает хитрые сообщения мошенников
- **📊 Статистика** - отслеживание нарушителей и предупреждений
- **⚙️ Гибкие настройки** - легко настраивается под ваши нужды
- **👮 Команды для админов** - ручное управление предупреждениями

## 🚀 Быстрый старт

### 1. Создание бота
1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 2. Установка
```bash
pip install pyTelegramBotAPI
```

### 3. Настройка
Откройте `main.py` и замените токен:
```python
TOKEN = "ВАШ_ТОКЕН_ОТ_BOTFATHER"
```

### 4. Запуск
```bash
python main.py
```

## 🏗️ Архитектура проекта

### Структура main.py:
```
main.py
├── КОНФИГУРАЦИЯ
│   ├── Токен бота
│   ├── Ключевые слова для блокировки
│   ├── Разрешенные домены
│   └── Настройки модерации
│
├── СИСТЕМЫ ХРАНЕНИЯ
│   ├── Антифлуд система
│   ├── Хранилище предупреждений
│
├── КЛАССЫ ОБРАБОТКИ
│   ├── AdDetector - детектор рекламы
│   │   ├── Проверка по паттернам
│   │   ├── Проверка на хитрые сообщения
│   │   ├── Проверка смешанных алфавитов
│   │   └── Проверка CAPS LOCK
│   │
│   └── Обработчики сообщений
│       ├── all_messages() - главный обработчик
│       └── check_ad() - проверка на рекламу
│
├── КОМАНДЫ БОТА
│   ├── Для всех: /start, /help
│   ├── Для админов: /warn, /unwarn, /reset_warns
│   └── Тестовые: /test, /stats
│
└── ЗАПУСК БОТА
```

### Основные классы и функции:

#### 1. **AdDetector** - ядро системы
```python
class AdDetector:
    def is_advertisement(text) -> bool
    def _check_spaced_text(text) -> bool      # "З а р а б о т о к"
    def _check_mixed_alphabet(text) -> bool   # "Пoлyчи дoxod"
    def _check_suspicious_emoji_patterns(text) -> bool
    def _check_caps_lock(text) -> bool
    def _check_spam_patterns(text) -> bool
```

#### 2. **Система хранения** (в памяти)
```python
last_message_time = {}        # Антифлуд: {user_id: timestamp}
user_warnings = defaultdict() # Предупреждения: {user_id: count}
```

#### 3. **Обработчики сообщений**
- `all_messages()` - обработка ВСЕХ сообщений с антифлудом
- `check_ad()` - детальная проверка на рекламу
- Командные обработчики (`/start`, `/help`, и т.д.)

#### 4. **Поток обработки сообщения**:
```
Сообщение → all_messages()
    ↓
Антифлуд проверка (3 сек)
    ↓
Пропуск команд (/команда)
    ↓
check_ad() → AdDetector
    ↓
Удаление + предупреждение (если реклама)
    ↓
Бан при 3+ предупреждениях
```

## 🎯 Что блокирует бот

### 🚫 Типы блокируемых сообщений:
- **Обычная реклама** - купить/продать/скидки/акции
- **Финансовые мошенничества** - кредиты/займы/инвестиции
- **Спам-ссылки** - подозрительные домены и ссылки
- **Фейковые угрозы** - "ваш аккаунт будет удален"
- **Хитрые сообщения** - с пробелами между буквами
- **Смешанные алфавиты** - кириллица+латиница в одном слове
- **CAPS LOCK** - сообщения в верхнем регистре
- **Флуд** - слишком частые сообщения

### 🎭 Примеры блокируемых сообщений:
```
💵💵💵  
Пoлyчи дoxod yжe ceгoдня  
100% gаrаnт

З а р а б о т о к 💸  
б е з   в л о ж е н и й  
п и ш и  в  л с

Ваш аккаунт Telegram будет удалён через 24 часа  
Подтвердите вход: tgram-auth.online
```

## 📋 Команды бота

### 👤 Для всех пользователей:
| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота и приветственное сообщение |
| `/help` | Список всех команд и возможностей |

### 👮 Для администраторов (в группах):
| Команда | Описание |
|---------|----------|
| `/warn` | Выдать предупреждение пользователю (ответом на сообщение) |
| `/unwarn` | Снять все предупреждения |
| `/reset_warns` | Сбросить предупреждения конкретному пользователю |
| `/stats` | Показать статистику предупреждений |
| `/test` | Протестировать детектор (в личных сообщениях) |

## ⚙️ Настройка и кастомизация

### 1. Ключевые слова для блокировки
Отредактируйте список `AD_KEYWORDS` в коде:
```python
AD_KEYWORDS = [
    r'купить', r'продам', r'скидк[а-я]*',
    r'заказ[а-я]*', r'доставк[а-я]*',
    r'бесплатно', r'акци[я-я]*',
    # Добавьте свои слова здесь
]
```

### 2. Настройки модерации
```python
WARN_LIMIT = 3          # Предупреждений до бана
TEMP_BAN_DURATION = 3600  # Время бана в секундах (1 час)
DELETE_MESSAGES = True  # Автоматическое удаление рекламы
```

### 3. Разрешенные домены
Добавьте безопасные домены в белый список:
```python
ALLOWED_DOMAINS = [
    'github.com', 'wikipedia.org',
    'google.com', 'youtube.com',
    # Ваши безопасные домены
]
```

## 🔧 Технические детали

### Алгоритмы обнаружения:
1. **Регулярные выражения** - поиск по 30+ шаблонам
2. **Анализ частоты** - защита от флуда (3 сек между сообщениями)
3. **Проверка алфавита** - обнаружение смешанных букв (кириллица+латиница)
4. **Анализ структуры** - пробелы между буквами в словах
5. **Эмодзи-детектор** - подозрительные комбинации денежных символов

### Система предупреждений:
```
1 предупреждение → Информационное сообщение
2 предупреждения → Строгое предупреждение  
3 предупреждения → Бан на 1 час + очистка истории
```

### Хранение данных:
- Временное хранение в оперативной памяти
- Автоматическая очистка при перезапуске
- Нет внешних зависимостей от БД

## 🚨 Добавление бота в группу

### Требуемые права:
1. Добавьте бота в группу
2. Назначьте администратором
3. Включите разрешения:
   - ✅ Удаление сообщений
   - ✅ Блокировка пользователей
   - ❌ Остальные права можно не давать

## 🛠️ Разработка

### Требования:
- Python 3.7 или выше
- Библиотека pyTelegramBotAPI
- Права администратора в группе

### Запуск в фоновом режиме:
```bash
# Используя screen
screen -S telegram-bot python main.py
# Ctrl+A, затем D для отключения
# screen -r telegram-bot для возврата

# Используя nohup
nohup python main.py > bot.log 2>&1 &
```

### Тестирование новых паттернов:
Используйте команду `/test` в личных сообщениях боту для проверки работы детектора.

## ⚠️ Важные моменты

### Ограничения:
- Бот работает только в группах и супергруппах
- В личных сообщениях функционал ограничен
- Данные хранятся только в оперативной памяти
- После перезапуска бота статистика сбрасывается

### Рекомендации:
1. Регулярно обновляйте список ключевых слов
2. Проверяйте ложные срабатывания
3. Настройте антифлуд под свои нужды
4. Добавляйте исключения для полезных ссылок

## 🤝 Вклад в проект

### Как помочь:
1. Добавляйте новые паттерны для обнаружения
2. Улучшайте алгоритмы детекции
3. Тестируйте и сообщайте о ложных срабатываниях
4. Предлагайте новые функции

## 📞 Обратная связь

Если у вас есть вопросы или предложения:
- Создайте Issue в репозитории
- Напишите разработчику
- Предложите улучшения кода

---

**⭐ Если вам нравится этот проект, поставьте звезду на GitHub!**

*Простой, но мощный антиспам бот в одном файле*
